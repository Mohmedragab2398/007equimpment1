# 🔧 دليل تثبيت Node.js و npm

## ❌ المشكلة:

الخطأ الذي تراه:
```
npm : The term 'npm' is not recognized...
```

يعني أن **Node.js** غير مثبت على جهازك.

---

## ✅ الحل: تثبيت Node.js

### الطريقة 1: التثبيت من الموقع الرسمي (موصى به)

1. **اذهب إلى الموقع الرسمي:**
   - https://nodejs.org/
   - أو https://nodejs.org/ar/ (بالعربية)

2. **حمّل النسخة LTS (الموصى بها):**
   - اضغط على زر "Download" للنسخة LTS
   - ستحمل ملف `.msi` (لـ Windows)

3. **شغّل ملف التثبيت:**
   - اضغط نقرتين على الملف المحمّل
   - اتبع خطوات التثبيت (Next, Next, Install)
   - ✅ تأكد من تحديد "Add to PATH" (عادة يكون محدد تلقائياً)

4. **أعد تشغيل Terminal:**
   - أغلق Terminal الحالي
   - افتح Terminal جديد

5. **تحقق من التثبيت:**
   ```bash
   node --version
   npm --version
   ```
   يجب أن ترى أرقام الإصدارات.

---

### الطريقة 2: استخدام Chocolatey (للمستخدمين المتقدمين)

إذا كان لديك Chocolatey مثبت:

```powershell
choco install nodejs-lts
```

---

### الطريقة 3: استخدام winget (Windows 11)

```powershell
winget install OpenJS.NodeJS.LTS
```

---

## ✅ بعد التثبيت:

### 1. تحقق من التثبيت:

```bash
node --version
npm --version
```

يجب أن ترى شيئاً مثل:
```
v20.11.0
10.2.4
```

### 2. تثبيت مكتبات المشروع:

```bash
cd D:\Download\007equimpment-main
npm install
```

### 3. تشغيل المشروع:

```bash
npm run dev
```

---

## 🔍 حل المشاكل الشائعة:

### المشكلة: بعد التثبيت، npm لا يزال غير معروف

**الحل:**
1. أعد تشغيل Terminal (أغلق وافتح جديد)
2. أعد تشغيل الكمبيوتر (إذا لم ينجح)
3. تحقق من PATH يدوياً:
   - ابحث عن "Environment Variables" في Windows
   - تأكد من وجود مسار Node.js في PATH

### المشكلة: خطأ في الصلاحيات

**الحل:**
- شغّل Terminal كـ Administrator
- أو استخدم `npm install --global` مع صلاحيات Admin

---

## 📦 ما الذي يتم تثبيته:

عند تثبيت Node.js، ستحصل على:
- ✅ **Node.js** - بيئة تشغيل JavaScript
- ✅ **npm** - مدير الحزم (Package Manager)
- ✅ **npx** - أداة تشغيل الحزم

---

## 🎯 الخطوات التالية بعد التثبيت:

1. ✅ تثبيت Node.js
2. ✅ تحقق من التثبيت (`node --version`)
3. ✅ تثبيت مكتبات المشروع (`npm install`)
4. ✅ تشغيل المشروع (`npm run dev`)

---

## 📞 للمساعدة:

إذا واجهت أي مشاكل:
1. تأكد من تثبيت النسخة LTS
2. أعد تشغيل Terminal بعد التثبيت
3. تحقق من PATH environment variable

---

**بعد تثبيت Node.js، ستعود للعمل بشكل طبيعي!** 🚀
