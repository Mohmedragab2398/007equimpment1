# 🔧 دليل إعداد Vercel للنشر التلقائي

## ✅ الخطوات المطلوبة لضمان النشر التلقائي:

### 1️⃣ التحقق من إعدادات Vercel:

1. اذهب إلى [Vercel Dashboard](https://vercel.com/dashboard)
2. اختر مشروعك: **007equimpment**
3. اذهب إلى **Settings** → **Git**
4. تأكد من:
   - ✅ **Production Branch**: يجب أن يكون `main`
   - ✅ **Auto-deploy**: يجب أن يكون مفعّل
   - ✅ **GitHub Integration**: يجب أن يكون مربوط بشكل صحيح

### 2️⃣ إعادة ربط المشروع (إذا لزم الأمر):

إذا كان النشر لا يزال لا يعمل:

1. اذهب إلى **Settings** → **Git**
2. اضغط على **Disconnect** (فصل الاتصال)
3. ثم اضغط على **Connect Git Repository**
4. اختر **GitHub** → **mohamedragab23/007equimpment**
5. تأكد من:
   - **Framework Preset**: `Other` أو `Vite`
   - **Root Directory**: `.` (النقطة)
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
   - **Production Branch**: `main`

### 3️⃣ اختبار النشر:

بعد إعادة الربط:

1. اذهب إلى **Deployments**
2. اضغط على **Redeploy** على آخر deployment
3. أو ادفع commit جديد إلى `main`:
   ```bash
   git add .
   git commit -m "test: Trigger Vercel deployment"
   git push github main
   ```

### 4️⃣ التحقق من ملف vercel.json:

✅ ملف `vercel.json` موجود وصحيح:
```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "npm install",
  ...
}
```

### 5️⃣ المشاكل الشائعة وحلولها:

#### ❌ المشكلة: النشر لا يبدأ تلقائياً
**الحل:**
- تحقق من أن آخر commit على branch `main`
- تأكد من أن Vercel مربوط بـ branch `main`
- تحقق من إعدادات GitHub webhook

#### ❌ المشكلة: البناء يفشل
**الحل:**
- تحقق من `package.json` - يجب أن يحتوي على `build` script
- تحقق من أن جميع dependencies موجودة
- راجع Build Logs في Vercel

#### ❌ المشكلة: الموقع لا يعمل بعد النشر
**الحل:**
- تحقق من `outputDirectory` في `vercel.json` (يجب أن يكون `dist`)
- تحقق من `routes` في `vercel.json`
- تأكد من وجود `index.html` في مجلد `dist`

### 6️⃣ إعادة النشر اليدوي:

إذا كان كل شيء صحيح ولكن النشر لا يبدأ:

1. اذهب إلى Vercel Dashboard
2. اختر المشروع
3. اذهب إلى **Deployments**
4. اضغط على **...** بجانب آخر deployment
5. اختر **Redeploy**

أو من سطر الأوامر:
```bash
# إضافة commit فارغ لتفعيل النشر
git commit --allow-empty -m "trigger: Force Vercel deployment"
git push github main
```

---

## 📞 للمساعدة:

إذا استمرت المشكلة:
1. راجع **Build Logs** في Vercel
2. تحقق من **GitHub Webhooks** في إعدادات المستودع
3. تأكد من أن Vercel لديه صلاحيات الوصول إلى المستودع

---

**✅ بعد تطبيق هذه الخطوات، يجب أن يعمل النشر التلقائي بشكل صحيح!**
